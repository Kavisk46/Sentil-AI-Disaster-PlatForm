"""Small, framework-agnostic helper functions with no business meaning.

Anything that encodes a decision about SentinelAI's domain (incidents,
imagery, detections, ...) does not belong here — this package is strictly
for generic helpers reused across otherwise-unrelated modules.
"""
