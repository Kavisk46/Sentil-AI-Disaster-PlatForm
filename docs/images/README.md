# Images

This directory holds static image assets (screenshots, exported diagrams,
logos) referenced from documentation in `docs/` and the root `README.md`.

Keep images reasonably sized and in a web-friendly format (PNG or SVG
preferred). Diagram sources that generate these images belong in
[`docs/diagrams/`](../diagrams/).

No image assets have been added yet.
